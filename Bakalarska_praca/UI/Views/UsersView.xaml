<Window x:Class="Bakalarska_praca.UI.Views.UsersView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Zoznam používateľov" Height="400" Width="600"
        WindowStartupLocation="CenterScreen">

    <Grid>
        <DataGrid ItemsSource="{Binding Users}" AutoGenerateColumns="False" CanUserAddRows="False"
                  SelectionMode="Single" IsReadOnly="True" Margin="10">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="50"/>
                <DataGridTextColumn Header="Login" Binding="{Binding Username}" Width="150"/>
                <DataGridTextColumn Header="Meno" Binding="{Binding Name}" Width="250"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>
